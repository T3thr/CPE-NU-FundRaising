# 🚀 คู่มือการเริ่มต้นใช้งานระบบ CPE Funds Hub

**Document:** GUIDE-GettingStarted.md  
**Version:** 1.0.0  
**Date:** 2026-01-11  
**Author:** CPE30 Development Team

---

## 📖 สารบัญ

1. [ความเป็นมาและความสำคัญ](#1-ความเป็นมาและความสำคัญ)
2. [สถาปัตยกรรมระบบ Authentication](#2-สถาปัตยกรรมระบบ-authentication)
3. [ขั้นตอนการติดตั้ง](#3-ขั้นตอนการติดตั้ง)
4. [การตั้งค่า Environment Variables](#4-การตั้งค่า-environment-variables)
5. [การ Setup ฐานข้อมูล Supabase](#5-การ-setup-ฐานข้อมูล-supabase)
6. [การทดสอบระบบ](#6-การทดสอบระบบ)
7. [บัญชีผู้ใช้และสิทธิ์](#7-บัญชีผู้ใช้และสิทธิ์)
8. [เหตุผลด้านความปลอดภัย](#8-เหตุผลด้านความปลอดภัย)
9. [Troubleshooting](#9-troubleshooting)
10. [FAQ](#10-faq)

---

## 1. ความเป็นมาและความสำคัญ

### 1.1 ทำไมต้องมีระบบนี้?

ก่อนหน้านี้ การจัดเก็บเงินกองกลางสาขา CPE ใช้ **Google Sheets** ซึ่งมีปัญหาหลายประการ:

| ปัญหาเดิม          | ผลกระทบ                   |
| ------------------ | ------------------------- |
| ตรวจสลิปด้วยตาคน   | ใช้เวลามาก มีโอกาสผิดพลาด |
| ไม่มีระบบแจ้งเตือน | คนค้างจ่ายไม่รู้ตัว       |
| ข้อมูลกระจัดกระจาย | ส่งต่อรุ่นน้องยาก         |
| ไม่มี Audit Trail  | ไม่รู้ว่าใครแก้ไขอะไร     |

### 1.2 ระบบใหม่แก้ปัญหาอย่างไร?

**CPE Funds Hub** ถูกออกแบบมาเพื่อ:

- ✅ **ตรวจสลิปอัตโนมัติ** ด้วย EasySlip API
- ✅ **แจ้งเตือนผ่าน LINE** ทันทีที่ได้รับเงิน
- ✅ **Dashboard Real-time** ดูได้ทุกที่ทุกเวลา
- ✅ **ความปลอดภัยระดับ Enterprise** (OWASP Standards)
- ✅ **รองรับการส่งต่อ** ด้วย Codebase ที่ดูแลง่าย

---

## 2. สถาปัตยกรรมระบบ Authentication

### 2.1 ทำไมถึงออกแบบแบบนี้?

ระบบนี้เป็น **"Private Web"** ที่:

- ❌ ไม่มีหน้าสมัครสมาชิก
- ❌ คนนอกไม่สามารถเข้าถึงได้
- ✅ มีบัญชีที่กำหนดไว้ล่วงหน้าเท่านั้น
- ✅ ทุกหน้าต้อง Login ก่อนเข้าใช้งาน

**เหตุผลหลัก:** ระบบนี้จัดการข้อมูลทางการเงินของสมาชิก ซึ่งเป็นข้อมูลอ่อนไหว การจำกัดการเข้าถึงจึงเป็นสิ่งจำเป็น

### 2.2 Authentication Flow

```
┌────────────────┐
│   User Input   │
│  Email/Password│
└───────┬────────┘
        │
        ▼
┌────────────────┐
│  Auth.js v5    │  ◄── ตรวจสอบ Credentials กับ
│  (Frontend)    │      AUTHORIZED_USERS ใน src/lib/auth.ts
└───────┬────────┘
        │
   ┌────┴────┐
   │ ถูกต้อง? │
   └────┬────┘
    Yes │  No
        │    └──────► แสดง Error "อีเมลหรือรหัสผ่านไม่ถูกต้อง"
        ▼
┌────────────────┐
│  JWT Session   │  ◄── หมดอายุใน 8 ชั่วโมง
│  Created       │      (สอดคล้องกับเวลาทำงาน)
└───────┬────────┘
        │
        ▼
┌────────────────┐
│  Access Granted│  ◄── Middleware ตรวจสอบ Role
│  (Home/Admin)  │      ก่อนเข้าแต่ละหน้า
└────────────────┘
```

### 2.3 ทำไมใช้ Hardcoded Credentials?

| แนวทาง         | ข้อดี                    | ข้อเสีย                                     | เหมาะกับระบบนี้? |
| -------------- | ------------------------ | ------------------------------------------- | ---------------- |
| **Hardcoded**  | ปลอดภัยสูงสุด, ควบคุมได้ | เปลี่ยน Password ต้อง Deploy ใหม่           | ✅ เหมาะมาก      |
| OAuth (Google) | สะดวก                    | คนนอกอาจเข้าถึงได้                          | ❌ ไม่เหมาะ      |
| สมัครสมาชิก    | ยืดหยุ่น                 | ต้องจัดการ Email Verification, ป้องกัน Spam | ❌ ไม่เหมาะ      |

**สรุป:** เนื่องจากผู้ใช้มีจำกัด (แค่ 2-3 คน) และเป็นข้อมูลทางการเงิน การ Hardcode จึงเป็นวิธีที่ปลอดภัยและจัดการง่ายที่สุด

---

## 3. ขั้นตอนการติดตั้ง

### 3.1 Prerequisites

- Node.js 20+ (ใช้ `node -v` เช็ค)
- npm หรือ pnpm
- Supabase Account (ฟรี)
- Git

### 3.2 Clone & Install

```bash
# 1. Clone repository
git clone https://github.com/YOUR_ORG/cpe-fundraising-refine.git
cd cpe-fundraising-refine

# 2. Install dependencies
npm install
```

---

## 4. การตั้งค่า Environment Variables

### 4.1 สร้างไฟล์ .env

```bash
# Copy จาก .env.example
cp .env.example .env
```

### 4.2 ค่าที่ต้องตั้ง (REQUIRED)

เปิด `.env` และตั้งค่าดังนี้:

```properties
# =============================================================================
# 🟢 APPLICATION
# =============================================================================
NODE_ENV=development
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# =============================================================================
# 🟢 AUTH.JS v5 (สำคัญมาก!)
# =============================================================================
# Generate: openssl rand -base64 32
# หรือใน PowerShell: [Convert]::ToBase64String((1..32 | ForEach-Object {Get-Random -Maximum 256}) -as [byte[]])
AUTH_SECRET="<ใส่ค่าที่ Generate มา>"
AUTH_TRUST_HOST=true

# =============================================================================
# 🟢 SUPABASE (ได้จาก https://supabase.com/dashboard)
# =============================================================================
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi...
```

### 4.3 วิธี Generate AUTH_SECRET

**Windows (PowerShell):**

```powershell
[Convert]::ToBase64String((1..32 | ForEach-Object {Get-Random -Maximum 256}) -as [byte[]])
```

**Linux/Mac:**

```bash
openssl rand -base64 32
```

⚠️ **คำเตือน:** อย่า Commit `.env` เข้า Git! (มันถูกใส่ใน `.gitignore` แล้ว)

---

## 5. การ Setup ฐานข้อมูล Supabase

### 5.1 ลำดับการรัน SQL

```
1. supabase/schema.sql          ← สร้างตารางทั้งหมด
2. supabase/fix_rls_and_seed_admin.sql  ← สร้าง Users และ Admin
```

### 5.2 วิธี Run SQL

1. ไปที่ **Supabase Dashboard** → เลือก Project ของคุณ
2. ไปที่ **SQL Editor** (เมนูซ้าย)
3. คลิก **New Query**
4. วาง SQL จากไฟล์ `supabase/schema.sql` → **Run**
5. คลิก **New Query** อีกครั้ง
6. วาง SQL จากไฟล์ `supabase/fix_rls_and_seed_admin.sql` → **Run**

### 5.3 ตรวจสอบว่าสำเร็จ

หลังรัน SQL แล้ว ให้ไปที่ **Table Editor** และตรวจสอบว่ามีตารางเหล่านี้:

- `organizations`
- `cohorts`
- `members`
- `payments`
- `admins`
- `audit_logs`

และไปที่ **Authentication → Users** ควรเห็น 2 users:

- `treasurer@cpe.nu.ac.th`
- `public@cpe.nu.ac.th`

---

## 6. การทดสอบระบบ

### 6.1 Start Development Server

```bash
npm run dev
```

เปิด Browser ไปที่ http://localhost:3000

### 6.2 ทดสอบ Login

1. เปิด http://localhost:3000/login
2. คลิกปุ่ม **"สมาชิกสาขา"** หรือ **"เหรัญญิก"** เพื่อกรอก Credentials อัตโนมัติ
3. คลิก **"เข้าสู่ระบบ"**

**Expected Result:**

- **Public User** → เข้าหน้า Home (ไม่สามารถเข้า /admin)
- **Super Admin** → เข้าหน้า Home และ /admin ได้

---

## 7. บัญชีผู้ใช้และสิทธิ์

### 7.1 บัญชีที่มีในระบบ

| บัญชี        | Email                    | Password            | Role        | สิทธิ์                           |
| ------------ | ------------------------ | ------------------- | ----------- | -------------------------------- |
| **เหรัญญิก** | `treasurer@cpe.nu.ac.th` | `CpeTreasurer2026!` | super_admin | ทุกอย่าง + Admin Panel           |
| **สมาชิก**   | `public@cpe.nu.ac.th`    | `CpePublicAccess!`  | public      | ดูข้อมูล, ชำระเงิน, ตรวจสอบสถานะ |

### 7.2 การเปลี่ยนรหัสผ่าน

เนื่องจากใช้ Hardcoded Credentials ในไฟล์ `src/lib/auth.ts`:

1. เปิดไฟล์ `src/lib/auth.ts`
2. หา `AUTHORIZED_USERS` (บรรทัดประมาณ 32)
3. แก้ไข `password` field
4. Deploy ใหม่

```typescript
const AUTHORIZED_USERS = [
  {
    email: "treasurer@cpe.nu.ac.th",
    password: "NEW_PASSWORD_HERE", // เปลี่ยนตรงนี้
    role: "super_admin",
    name: "เหรัญญิก CPE",
  },
  // ...
];
```

⚠️ **สำคัญ:** หลังเปลี่ยน Password ในโค้ดแล้ว ต้องไปเปลี่ยนใน Database ด้วย (หรือ Reset database ใหม่)

---

## 8. เหตุผลด้านความปลอดภัย

### 8.1 ทำไมถึงปลอดภัย?

ระบบนี้ปฏิบัติตามมาตรฐาน **OWASP Top 10** ซึ่งเป็นรายการช่องโหว่ที่พบบ่อยที่สุด 10 อันดับแรกของเว็บแอปพลิเคชัน:

| ช่องโหว่                        | การป้องกัน                           |
| ------------------------------- | ------------------------------------ |
| A01 - Broken Access Control     | Role-based access ผ่าน Middleware    |
| A02 - Cryptographic Failures    | HTTPS, Secure Cookies, JWT           |
| A03 - Injection                 | Parameterized queries (Supabase)     |
| A04 - Insecure Design           | Private by default, No registration  |
| A05 - Security Misconfiguration | Security Headers (HSTS, CSP)         |
| A07 - Auth Failures             | Hardcoded credentials, Short session |

### 8.2 Security Headers ที่ใช้

ทุก Request จะถูกเพิ่ม Headers เหล่านี้โดยอัตโนมัติ:

```
X-XSS-Protection: 1; mode=block
X-Frame-Options: DENY
X-Content-Type-Options: nosniff
Referrer-Policy: strict-origin-when-cross-origin
Strict-Transport-Security: max-age=31536000
Content-Security-Policy: default-src 'self'; ...
```

### 8.3 Session Security

- **JWT Token** หมดอายุใน **8 ชั่วโมง** (สอดคล้องกับเวลาทำงาน)
- **Token Refresh** ทุก 1 ชั่วโมง
- **HttpOnly Cookie** ป้องกัน XSS Attack
- **SameSite Cookie** ป้องกัน CSRF Attack

### 8.4 Row Level Security (RLS)

Database ถูกตั้ง RLS ไว้ ทำให้:

- **Public User** ดูได้เฉพาะข้อมูลส่วนตัว
- **Admin** ดูได้ทั้งหมด แต่แก้ไขได้เฉพาะ Cohort ที่รับผิดชอบ
- **Super Admin** ทำได้ทุกอย่าง

---

## 9. Troubleshooting

### 9.1 MissingSecret Error

```
[auth][error] MissingSecret: Please define a `secret`.
```

**สาเหตุ:** ไม่มี `AUTH_SECRET` ใน `.env`

**แก้ไข:** เพิ่ม `AUTH_SECRET="<random-string>"` ใน `.env` แล้ว Restart server

### 9.2 MissingCSRF Error

```
[auth][error] MissingCSRF: CSRF token was missing
```

**สาเหตุ:** Cookie ถูก Block (มักเกิดเมื่อใช้ custom cookie config ที่ต้องการ HTTPS)

**แก้ไข:** อัพเดท `src/lib/auth.ts` ให้ Auth.js จัดการ cookie เอง (ลบ custom `cookies` config ออก)

### 9.3 Cannot Access /admin

**สาเหตุ:** Login ด้วย Public User ซึ่งไม่มีสิทธิ์เข้า Admin Panel

**แก้ไข:** Login ด้วย `treasurer@cpe.nu.ac.th` แทน

### 9.4 Database Connection Failed

**สาเหตุ:** Supabase credentials ไม่ถูกต้อง

**แก้ไข:**

1. ไปที่ Supabase Dashboard → Project Settings → API
2. Copy `Project URL` → ใส่ใน `NEXT_PUBLIC_SUPABASE_URL`
3. Copy `anon public` key → ใส่ใน `NEXT_PUBLIC_SUPABASE_ANON_KEY`
4. Copy `service_role` key → ใส่ใน `SUPABASE_SERVICE_ROLE_KEY`

---

## 10. FAQ

### Q: ถ้าเหรัญญิกรุ่นใหม่มารับช่วงต่อ ต้องทำอย่างไร?

**A:**

1. เปลี่ยน Password ในไฟล์ `src/lib/auth.ts`
2. Reset database (รัน SQL ใหม่)
3. Deploy ใหม่
4. ส่งมอบ `.env` file ให้คนใหม่

### Q: ข้อมูลเก็บที่ไหน?

**A:**

- **Database**: Supabase (PostgreSQL) - เก็บข้อมูลสมาชิก, การชำระเงิน
- **Authentication**: Auth.js (JWT) - เก็บ Session ใน Cookie browser
- **Files/Slips**: Supabase Storage (ถ้าใช้)

### Q: Session หมดอายุแล้วต้องทำอย่างไร?

**A:** เพียงแค่ Login ใหม่ ระบบจะสร้าง Session ให้อัตโนมัติ (หมดอายุทุก 8 ชั่วโมง)

### Q: อยากเพิ่ม User ใหม่ทำอย่างไร?

**A:** เพิ่มใน `AUTHORIZED_USERS` array ในไฟล์ `src/lib/auth.ts`:

```typescript
{
  email: "new-user@cpe.nu.ac.th",
  password: "NewSecurePassword123!",
  role: "public",  // หรือ "super_admin"
  name: "ชื่อผู้ใช้ใหม่",
},
```

### Q: ทำไมไม่ใช้ Supabase Auth แทน?

**A:** ระบบนี้ต้องการ:

- จำกัดผู้ใช้เฉพาะคนในสาขา
- ไม่มีหน้าสมัครสมาชิก
- ควบคุม Credentials ได้ 100%

Supabase Auth ออกแบบมาสำหรับ App ที่มีผู้ใช้สาธารณะ ไม่เหมาะกับ Private Internal Tool แบบนี้

---

## 📋 Checklist ก่อน Go Live

- [ ] `.env` ตั้งค่าครบถ้วน
- [ ] `AUTH_SECRET` ใช้ค่า Random (ไม่ใช่ค่าจาก .env.example)
- [ ] Database มี Table ครบถ้วน
- [ ] Users ถูก Seed เรียบร้อย
- [ ] Login ได้ทั้ง Public และ Super Admin
- [ ] Admin Panel เข้าได้เฉพาะ Super Admin
- [ ] Security Headers ถูกต้อง (ใช้ https://securityheaders.com ตรวจสอบ)

---

**🎉 ยินดีต้อนรับสู่ CPE Funds Hub!**

_พัฒนาโดยทีมบอร์ดบริหารสาขาวิศวกรรมคอมพิวเตอร์ รุ่นที่ 30_  
_เพื่อให้ระบบการเก็บเงินกองกลางมีประสิทธิภาพและโปร่งใสมากยิ่งขึ้น_
