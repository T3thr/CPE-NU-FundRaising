# 📝 สรุปการปรับปรุง Critical Issues (2026-01-10)

เอกสารนี้สรุปการแก้ไข Critical Issues ตามที่ได้รับมอบหมาย

---

## ✅ 1. แก้ไข Settings Tab - เชื่อมต่อ Supabase จริง

### ปัญหา:

- Line Messaging แสดง "ไม่ได้เชื่อมต่อ" ทั้งที่มี access token ใน `.env`
- EasySlip ไม่แสดงโควต้าจริงจาก API

### แก้ไข:

**ไฟล์:** `src/app/(admin)/admin/_actions/admin-actions.ts`

1. **`getEasySlipStatus()`** - เปลี่ยนเป็นเรียก EasySlip API `/me` endpoint จริง พร้อม error handling ละเอียด
2. **`getLineMessagingStatus()`** - แยกตรวจสอบ 2 ส่วน:
   - `channelConfigured`: ตรวจสอบว่ามี `LINE_CHANNEL_ACCESS_TOKEN` หรือไม่
   - `targetConfigured`: ตรวจสอบว่ามี `LINE_GROUP_ID` หรือ `LINE_USER_ID` หรือไม่

**ไฟล์:** `src/app/(admin)/admin/settings/_components/SettingsContent.tsx`

- อัปเดต UI แสดงสถานะละเอียด 2 บรรทัด:
  - ✅/❌ Channel Access Token
  - ✅/❌ Group/User ID

---

## ✅ 2. Smart Import - รองรับชื่อเล่นอย่างชาญฉลาด

### ปัญหา:

- ข้อมูลแบบ `นาย ธีรภัทร ภู่ระย้า	เซ็นเตอร์	66362416` ไม่สามารถแยกชื่อเล่น "เซ็นเตอร์" ได้

### แก้ไข:

**ไฟล์:** `src/utils/validation/index.ts`

ปรับ `parsePasteData()` function ให้:

1. เพิ่ม `isLikelyNickname()` helper function - ตรวจจับว่าคำนั้นน่าจะเป็นชื่อเล่นหรือไม่
   - คำภาษาไทยสั้นๆ 1-10 ตัวอักษร ที่ไม่ใช่คำนำหน้า
   - คำภาษาอังกฤษ 2-15 ตัวอักษร
2. รองรับ 2 รูปแบบ:
   - **แยกคอลัมน์:** `ชื่อ-นามสกุล | ชื่อเล่น | รหัสนิสิต`
   - **รวมในชื่อ:** `ธีรภัทร ภู่ระย้า เซ็นเตอร์` (ถ้าคำสุดท้ายเป็นชื่อเล่น จะแยกออกมา)

### ตัวอย่างที่รองรับ:

```
นาย ธีรภัทร ภู่ระย้า	เซ็นเตอร์	66362416   ✅ ชื่อเล่น = "เซ็นเตอร์"
66362416	นายสมชาย ใจดี โน๊ต                   ✅ ชื่อเล่น = "โน๊ต"
66362417	นางสาวสมหญิง รักเรียน	Mint         ✅ ชื่อเล่น = "Mint"
```

---

## ✅ 3. เพิ่ม Organization Tab ใหม่

### ไฟล์ใหม่:

- `src/app/(admin)/admin/organization/page.tsx`
- `src/app/(admin)/admin/organization/_components/OrganizationContent.tsx`

### ฟีเจอร์:

- **ตั้งค่าข้อมูลภาควิชา/องค์กร** แบบ No-Code
- **ตั้งค่าบัญชีธนาคาร** สำหรับรับชำระเงิน
- **รองรับการสร้างใหม่** หากยังไม่มีข้อมูล
- **บันทึกแบบ Real-time** ผ่าน Server Actions
- **UI/UX สวยงาม** ตามมาตรฐาน STANDARD-TailwindCSS.md

### Server Actions ที่เพิ่ม:

**ไฟล์:** `src/app/(admin)/admin/_actions/admin-actions.ts`

```typescript
export interface OrganizationData { ... }
export async function getOrganization(): Promise<OrganizationData | null>
export async function updateOrganization(orgId: string, data: {...})
export async function createOrganization(data: {...})
```

### Navigation:

- เพิ่มเมนู "องค์กร" ใน Admin Sidebar
- เพิ่ม breadcrumb label

---

## 📁 ไฟล์ที่เปลี่ยนแปลง

| ไฟล์                                                                     | การเปลี่ยนแปลง                                        |
| ------------------------------------------------------------------------ | ----------------------------------------------------- |
| `src/app/(admin)/admin/_actions/admin-actions.ts`                        | เพิ่ม Organization actions, ปรับ EasySlip/Line status |
| `src/app/(admin)/admin/settings/_components/SettingsContent.tsx`         | อัปเดต UI สถานะ Line Messaging                        |
| `src/utils/validation/index.ts`                                          | ปรับ parsePasteData รองรับชื่อเล่น                    |
| `src/app/(admin)/admin/organization/page.tsx`                            | **ใหม่**                                              |
| `src/app/(admin)/admin/organization/_components/OrganizationContent.tsx` | **ใหม่**                                              |
| `src/config/app.config.ts`                                               | เพิ่มเมนู Organization                                |
| `src/components/layout/Sidebar.tsx`                                      | เพิ่ม breadcrumb label                                |

---

## 🧪 วิธีทดสอบ

1. **Line Messaging Status**: ไป `/admin/settings` ตรวจสอบว่า:

   - แสดง ✅ Channel Access Token (เพราะมีใน .env)
   - แสดง ❌ Group/User ID (เพราะยังว่างอยู่)

2. **Smart Import ชื่อเล่น**: ไป `/admin/members` > Smart Import:

   - วางข้อมูล: `นาย ธีรภัทร ภู่ระย้า	เซ็นเตอร์	66362416`
   - ตรวจสอบว่า Preview แสดงชื่อเล่น "เซ็นเตอร์" ถูกต้อง

3. **Organization Tab**: ไป `/admin/organization`:
   - กรอกข้อมูลและบันทึก
   - ตรวจสอบว่าบันทึกลง Supabase จริง

---

## 📌 หมายเหตุ

- ทุกการเปลี่ยนแปลงเป็นไปตาม Best Practice ของ Next.js 15+ Server Actions
- UI ออกแบบตาม `STANDARD-TailwindCSS.md`
- รองรับ No-Code Admin ตาม `DESIGN-Database&DataEntry.md`
