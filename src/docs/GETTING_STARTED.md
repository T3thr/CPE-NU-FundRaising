# 🚀 คู่มือเตรียมความพร้อมก่อนเริ่มใช้งานจริง (Getting Started Guide)

เอกสารนี้จะแนะนำขั้นตอนที่ต้องทำหลังจาก deploy ระบบเสร็จเรียบร้อย เพื่อให้พร้อมสำหรับการใช้งานจริง โดยเน้นที่การเตรียมฐานข้อมูลและการนำเข้าสมาชิก

---

## 1. 🗄️ เตรียมฐานข้อมูล (Database Setup)

เนื่องจากระบบใช้ Supabase ต้องตรวจสอบว่าตารางต่างๆ ถูกสร้างครบถ้วนแล้ว

### วิธีตรวจสอบ:

1. เข้าไปที่ **Supabase Dashboard** > **Table Editor**
2. ตรวจสอบว่ามี Tables เหล่านี้ครบหรือไม่:
   - `organizations` (ข้อมูลภาควิชา)
   - `cohorts` (ข้อมูลรุ่นปีการศึกษา)
   - `members` (รายชื่อนิสิต)
   - `payments` (ประวัติการชำระเงิน)
   - `admins` (ผู้ดูแลระบบ)

### ⚠️ หากยังไม่มีตาราง:

ให้ไปที่ **SQL Editor** ใน Supabase และรันไฟล์ `supabase/schema.sql` ทั้งหมด

---

## 2. 🏢 สร้างข้อมูลเริ่มต้น (Initial Data Seeding)

ระบบต้องการข้อมูล "องค์กร" (Organization) และ "รุ่น" (Cohort) อย่างน้อย 1 รุ่นจึงจะทำงานได้

### วิธีสร้างข้อมูลเริ่มต้น (แนะนำ):

ให้รัน SQL Script ที่เตรียมไว้ให้ใน `supabase/quick-setup.sql` ผ่าน SQL Editor ของ Supabase

**สิ่งที่ Script นี้จะทำ:**

1. สร้าง Organization: "ภาควิชาวิศวกรรมคอมพิวเตอร์"
2. สร้าง Cohort เริ่มต้น: "CPE30" (ปีการศึกษา 2568)
3. ตั้งค่าค่าธรรมเนียมเริ่มต้น (70 บาท)

> **หมายเหตุ:** คุณสามารถแก้ไขชื่อรุ่นหรือปีการศึกษาในไฟล์ SQL นี้ก่อนรันได้

---

## 3. 👥 การนำเข้าสมาชิก (Import Members)

ขั้นตอนนี้สำคัญที่สุดเพื่อให้ระบบพร้อมเก็บเงิน

### วิธีเตรียมข้อมูลสำหรับการ Import:

1. เตรียมไฟล์ **Excel** หรือ **Google Sheets** ที่มีรายชื่อนิสิต
2. คอลัมน์ที่ต้องมี (เรียงตามลำดับ หรือก๊อปปี้ไปวาง):
   - **รหัสนิสิต** (จำเป็น) - เช่น 6636xxxx
   - **คำนำหน้า** (ถ้ามี) - นาย/นางสาว
   - **ชื่อจริง** (จำเป็น)
   - **นามสกุล** (จำเป็น)
   - **ชื่อเล่น** (ถ้ามี)

### วิธีนำเข้าเข้าระบบ:

1. เปิดเว็บแอป และ Login เข้าสู่ระบบ Admin
2. ไปที่เมนู **"สมาชิก" (Members)**
3. คลิกปุ่ม **"นำเข้าข้อมูล (Smart Import)"** หรือ **"Import"**
4. ก๊อปปี้ข้อมูลจาก Excel (Ctrl+C) แล้วมาวาง (Ctrl+V) ในหน้าต่างที่ปรากฏ
5. ระบบจะแสดงตัวอย่างข้อมูล ตรวจสอบความถูกต้อง แล้วกด **"ยืนยันการนำเข้า"**

---

## 4. ⚙️ ตั้งค่าการเชื่อมต่ออื่นๆ (Optional)

หากต้องการให้ฟีเจอร์อัตโนมัติทำงานสมบูรณ์:

### A. ตั้งค่าตรวจสอบ Slip อัตโนมัติ (EasySlip)

1. สมัครสมาชิกที่ [EasySlip.com](https://easyslip.com)
2. นำ **API Key** มาใส่ใน `.env` ของโปรเจกต์:
   ```env
   EASYSLIP_API_KEY=your_api_key_here
   ```
3. ตรวจสอบสถานะได้ที่เมนู **"ตั้งค่า" (Settings)**

### B. ตั้งค่า Line Notify/Messaging (ถ้าต้องการ)

1. นำ **Channel Access Token** มาใส่ใน `.env`:
   ```env
   LINE_CHANNEL_ACCESS_TOKEN=your_token_here
   ```

---

## 🎉 พร้อมใช้งานแล้ว!

เมื่อทำครบทุกขั้นตอน คุณสามารถเริ่ม:

1. แจ้งให้นิสิตชำระเงิน
2. รอตรวจสอบ Slip (หรือให้ระบบตรวจให้อัตโนมัติ)
3. ดูรายงานสรุปยอดเงินได้ทันทีที่หน้า Dashboard
