# 📋 CHANGELOG: Smart Data Management Implementation V1.0

(บันทึกการแก้ไข: ระบบจัดการข้อมูลอัจฉริยะ)

**วันที่:** 2026-01-10  
**ผู้ดำเนินการ:** AI Assistant  
**เอกสารอ้างอิง:** `DESIGN-Database&DataEntry.md`, `PROJECT-Background&Mission.md`

---

## 🎯 สรุปการแก้ไข (Summary)

การอัปเดตครั้งนี้เป็นการปรับปรุงระบบจัดการสมาชิก (Members Management) ให้รองรับแนวคิด **"Low-Code, High-Impact"** เพื่อให้ Admin รุ่นน้องสามารถจัดการข้อมูลได้ง่ายขึ้นโดยไม่ต้องเขียนโค้ด

---

## 📂 1. Database Schema Changes (`supabase/schema.sql`)

### 1.1 ปรับปรุง `cohorts` Table

- **เพิ่ม `slug`:** สำหรับ URL-friendly identifier (เช่น `cpe30`)
- **เพิ่ม `config` (JSONB):** เก็บตั้งค่ายืดหยุ่นของแต่ละรุ่น (เช่น บัญชีธนาคาร, LINE Group)
- **ปรับ Default:** `start_month` เป็น 7 (กรกฎาคม), `end_month` เป็น 3 (มีนาคม)

### 1.2 ปรับปรุง `members` Table

- **เปลี่ยน `full_name` → `first_name` + `last_name`:** แยกชื่อ-นามสกุลออกจากกันเพื่อความยืดหยุ่น
- **เพิ่ม `title`:** คำนำหน้าชื่อ (นาย/นางสาว)
- **เพิ่ม `profile_data` (JSONB):** เก็บข้อมูลเสริมที่อาจเปลี่ยนไปตามรุ่น (เช่น สายรหัส, Facebook)
- **เปลี่ยน `is_active` → `status`:** รองรับสถานะที่หลากหลาย (`active`, `resigned`, `graduated`)
- **ปรับ `student_id`:** เป็น `VARCHAR(8)` พร้อม Comment อธิบาย Format

---

## 🛠 2. Validation Utilities (`src/utils/validation/index.ts`)

### 2.1 เพิ่มฟังก์ชันใหม่

| Function                 | คำอธิบาย                                                              |
| ------------------------ | --------------------------------------------------------------------- |
| `isValidCPEStudentId()`  | ตรวจสอบรหัสนิสิต CPE (ต้องมีรหัสสาขา 36)                              |
| `getCPEGeneration()`     | คำนวณรุ่น CPE จากรหัสนิสิต (สูตร: `YY - 36`)                          |
| `getCPEGenerationText()` | ได้ข้อความ "CPE30", "CPE31"                                           |
| `getNUGeneration()`      | ได้ข้อความ "NU66", "NU67"                                             |
| `parsePasteData()`       | **Smart Import:** แปลงข้อความที่ก็อปปี้จาก Excel เป็น Array ของสมาชิก |

### 2.2 ปรับปรุง `validateMemberData()`

- รองรับ `firstName` + `lastName` แทน `fullName`

---

## 🖥 3. UI Components

### 3.1 สร้าง `SmartImportModal.tsx` (ใหม่)

**Location:** `src/app/(admin)/admin/members/_components/SmartImportModal.tsx`

- **Copy-Paste Workflow:** Admin ก็อปปี้ข้อมูลจาก Excel/Sheets มาวางได้เลย
- **Smart Detection:** ระบบตรวจจับอัตโนมัติว่า Column ไหนคือ รหัสนิสิต, ชื่อ, นามสกุล
- **Validation Preview:** โชว์ตารางตัวอย่างก่อนนำเข้า พร้อม Highlight ข้อมูลที่มีปัญหา
- **UI/UX:** ใช้ Framer Motion สำหรับ Animation, ออกแบบตามมาตรฐาน `STANDARD-TailwindCSS.md`

### 3.2 ปรับปรุง `MembersListContent.tsx`

**สิ่งที่เปลี่ยน:**

- **เพิ่มปุ่ม "Smart Import":** เปิด Modal สำหรับนำเข้าข้อมูลจาก Excel
- **เพิ่มปุ่ม "Export CSV":** ดาวน์โหลดรายชื่อสมาชิกเป็นไฟล์ CSV (รองรับภาษาไทย)
- **แสดง CPE Generation:** แสดง Badge "CPE30" ข้างหัวข้อ
- **ปรับ Mock Data:** ใช้รหัสนิสิต `66360xxx` (CPE30) และโครงสร้างใหม่
- **ปรับ Filter:** รองรับสถานะ `resigned` (ลาออก) และ `graduated` (จบแล้ว)
- **ปรับ Table:** แสดง `คำนำหน้า + ชื่อ + นามสกุล` แทน `fullName`

---

## ✅ 4. สิ่งที่ต้องทำต่อ (TODO)

1. **รัน SQL Migration:** นำ SQL ใน `supabase/schema.sql` ไปรันใน Supabase SQL Editor
2. **เชื่อมต่อ Supabase จริง:** แทน Mock Data ด้วย `useList` จาก Refine
3. **Server Actions:** สร้าง `importMembersAction` สำหรับ Insert หลายรายการพร้อมกัน
4. **ปรับหน้า Create Member:** ให้รองรับ Schema ใหม่ (firstName, lastName, profile_data)

---

## 📚 เอกสารที่เกี่ยวข้อง

- [DESIGN-Database&DataEntry.md](./DESIGN-Database&DataEntry.md)
- [PROJECT-Background&Mission.md](./PROJECT-Background&Mission.md)
- [SYSTEM-Validation&BusinessRules.md](./SYSTEM-Validation&BusinessRules.md)
- [STANDARD-TailwindCSS.md](./STANDARD-TailwindCSS.md)

---

**หมายเหตุ:** การแก้ไขนี้เป็นการวาง Foundation สำหรับระบบ "Low-Code" ที่ Admin รุ่นน้องจะใช้งานได้ง่าย โดยเน้นการ Import/Export ข้อมูลแบบ Excel-First แทนการพิมพ์ทีละรายการ
