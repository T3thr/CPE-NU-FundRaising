# 🧠 Database & Smart Data Management Design

(เอกสารออกแบบฐานข้อมูลและระบบจัดการข้อมูลอัจฉริยะ)

---

## 🎯 1. แนวคิดหลัก (Core Concept) - "Low Code, High Impact"

เพื่อให้ระบบนี้ยั่งยืนสำหรับ Admin รุ่นน้องที่อาจไม่ใช่โปรแกรมเมอร์ เราจะใช้แนวคิด **"Excel-First Experience"**

- **Source of Truth:** เก็บข้อมูลหลักที่ **Supabase (PostgreSQL)** เพื่อความปลอดภัยและ Data Integrity
- **User Interface:** ทำหน้าจัดการให้รู้สึก "คุ้นเคย" เหมือนใช้ Google Sheets
- **Integration:** เน้นการ **Import/Export** ที่ฉลาดและง่าย แทนการทำ Real-time Sync ที่ซับซ้อนและเสี่ยงต่อการพัง

---

## 🏛️ 2. Database Schema Design (PostgreSQL)

ออกแบบ Schema ให้รองรับ **Multi-Tenancy** (หลายรุ่น) และมีความยืดหยุ่นสูง

### 2.1 Table: `cohorts` (รุ่น/ปีการศึกษา)

ตารางหลักสำหรับแยกข้อมูลของแต่ละรุ่นออกจากกัน

```sql
CREATE TABLE cohorts (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name TEXT NOT NULL,                -- ชื่อรุ่น (เช่น "CPE30")
    academic_year INT NOT NULL,        -- ปีการศึกษา (เช่น 2566)
    slug TEXT UNIQUE NOT NULL,         -- URL slug (เช่น "cpe30", "cpe31")
    config JSONB DEFAULT '{}',         -- เก็บตั้งค่ารุ่น (ค่าปรับ, ค่าสาขา, เดือนเริ่มเก็บ)
    is_active BOOLEAN DEFAULT true,
    created_at TIMESTAMPTZ DEFAULT NOW()
);
```

### 2.2 Table: `members` (นิสิต/สมาชิก)

หัวใจสำคัญของระบบ เน้นการเก็บข้อมูลที่ยืดหยุ่น

```sql
CREATE TABLE members (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    cohort_id UUID REFERENCES cohorts(id) ON DELETE CASCADE,
    student_id VARCHAR(8) NOT NULL,    -- รหัสนิสิต (YYMMXXXX) - บังคับ Unique ในรุ่น
    title TEXT,                        -- คำนำหน้า (นาย/นางสาว)
    first_name TEXT NOT NULL,          -- ชื่อจริง
    last_name TEXT NOT NULL,           -- นามสกุล
    email TEXT,                        -- อีเมล (สำหรับส่งใบเสร็จ)
    phone TEXT,                        -- เบอร์โทร (optional)
    line_id TEXT,                      -- Line ID (สำหรับการตามหนี้)

    -- 🚀 Smart Field: ข้อมูลยืดหยุ่นที่อาจเปลี่ยนไปตามรุ่น
    profile_data JSONB DEFAULT '{}',   -- เก็บข้อมูลอื่นๆ เช่น "ชื่อเล่น", "สายรหัส", "Facebook"

    status TEXT DEFAULT 'active',      -- active, resigned, graduated
    created_at TIMESTAMPTZ DEFAULT NOW(),

    -- Composite Unique Key: ห้ามมีรหัสนิสิตซ้ำในรุ่นเดียวกัน
    UNIQUE(cohort_id, student_id)
);
```

---

## ⚡ 3. Low-Code Data Management Solutions

เราจะแก้ Pain Point เรื่องการกรอกข้อมูลด้วย 3 ฟีเจอร์หลัก:

### ✅ Solution A: "Smart Copy-Paste Import" (แนะนำสูงสุด)

วิธีที่ง่ายที่สุด เร็วที่สุด และ Error น้อยที่สุด

- **Workflow:**
  1.  Admin ก็อปปี้ตารางรายชื่อจาก Excel/Google Sheets ของคณะมา (Ctrl+C)
  2.  มาที่หน้าเว็บ กดปุ่ม "Import Members" -> วางลงใน Text Area (Ctrl+V)
  3.  ระบบจะ **Parse** ข้อมูลให้อัตโนมัติ (แยก Column ด้วย Tab)
  4.  **Smart Mapping:** ระบบเดาว่า Column ไหนคือ รหัสนิสิต, ชื่อ, นามสกุล (ถ้าเดาผิด Admin ลากเปลี่ยนเองได้)
  5.  **Validation Preview:** ระบบโชว์ตารางตัวอย่าง และ **ไฮไลท์สีแดง** ถ้ารหัสนิสิตผิด Format หรือซ้ำ
  6.  กดยืนยัน -> ข้อมูลเข้า Database ทันที

### ✅ Solution B: "Live Sheet Link" (ลูกเล่นเสริม)

สำหรับ Admin ที่อยากใช้ Google Sheets เป็นหลักในการเตรียมข้อมูล

- **Workflow:**
  1.  Admin ทำ Google Sheet เป็น "Publish to Web" (CSV Format) - _ไม่ต้องใช้ API Key/OAuth_
  2.  นำ URL มาแปะในหน้าตั้งค่ารุ่น
  3.  กดปุ่ม **"Sync from Sheet"**
  4.  ระบบจะไปดึง CSV มาเทียบกับ Database:
      - _Found 5 new students_
      - _Found 3 updates (Name changed)_
  5.  Admin กดยืนยันการอัปเดต

### ✅ Solution C: "One-Click Export for Reports"

- มีปุ่ม "Export to Excel/CSV" ในทุกตาราง เพื่อให้นำข้อมูลไปทำรายงานส่งคณะได้ทันที

---

## 🤖 4. AI & Automation Support (Future-Proofing)

เพื่อรองรับการทำงานในอนาคต เราจะวางโครงสร้างให้ AI สามารถเข้ามาช่วยได้:

- **Natural Language Query:** ออกแบบ Table Name/Column Name ให้สื่อความหมาย (Semantic Naming) เพื่อให้ AI Agent ในอนาคตสามารถเขียน Query ดึงข้อมูลได้ง่าย (เช่น "ขอรายชื่อคนที่ค้างจ่าย 3 เดือน")
- **Metadata Tagging:** ใช้ JSONB (`profile_data`) เพื่อให้ AI สามารถเพิ่ม Tag พิเศษให้นิสิตได้โดยไม่ต้องแก้ Table (เช่น `{"risk_level": "high"}` สำหรับคนจ่ายช้าบ่อยๆ)

---

## 🛠 5. Implementation Stack (Next.js 15 + Refine)

- **Frontend Library:** `useTable`, `useImport` จาก Refine Core (มี CSV Parser ในตัว)
- **UI Component:** สร้าง `SmartImportModal` โดยใช้ Tailwind CSS
- **Function:** `parsePasteData(text)` - ฟังก์ชันแยก Text เป็น Array of Objects

---

> **สรุป:** การออกแบบนี้เน้น "ความง่ายหน้างาน" (Frontend UX) มากกว่าความซับซ้อนหลังบ้าน (Backend Sync) ทำให้ Admin รุ่นต่อไปไม่ต้องปวดหัวเรื่องการ Maintain API Connection แต่ยังคงความสะดวกเหมือนใช้ Excel ครับ
