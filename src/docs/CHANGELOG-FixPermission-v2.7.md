# CHANGELOG: Fix Permission Denied (42501) & Hybrid Auth Security

**Version:** 2.7
**Date:** 2026-01-11
**Author:** Antigravity (AI Assistant)

## 🚨 ปัญหาที่พบ (The Issue)

ผู้ใช้พบข้อผิดพลาด `Error 42501: new row violates row-level security policy` เมื่อพยายามสร้าง "รุ่น" (Cohort) หรือทำรายการแก้ไขใน Admin Dashboard ทั้งที่เข้าสู่ระบบด้วยบัญชีเหรัญญิก (`super_admin`)

### 🔍 สาเหตุรากฐาน (Root Cause Analysis)

ปัญหานี้เกิดจากความไม่เชื่อมต่อกันของสถาปัตยกรรม **Hybrid Authentication**:

1.  **Frontend Auth (NextAuth):** ใช้ระบบ Login แบบ Credentials (Hardcoded Users) ซึ่งสะดวกและรวดเร็วสำหรับ Private Web
2.  **Database Auth (Supabase):** ใช้ระบบ RLS (Row Level Security) ที่เข้มงวด ซึ่งต้องการ JWT Token ที่ถูกต้องจาก Supabase Auth เพื่อระบุตัวตน User
3.  **The Disconnect:** เมื่อ NextAuth เข้าสู่ระบบ มันสร้าง Cookie ของตัวเอง (`authjs.session-token`) แต่ **ไม่ได้สร้าง** Cookie ของ Supabase Authentication ดังนั้นเมื่อ Server Action พยายามติดต่อ Database ผ่าน `createSupabaseServerClient`, Supabase จะมองเห็น Request นั้นมาจาก **Anonymous User** ซึ่งไม่มีสิทธิ์เขียนข้อมูล (Write Permission) ตาม RLS Policy

## 🛠️ การแก้ไขและปรับปรุง (Implementation Details)

เพื่อให้ระบบทำงานได้ตามมาตรฐาน Security โดยไม่ต้องรื้อระบบ Auth ทั้งหมด เราได้ใช้แนวทาง **Secure Service Role Execution w/ App-Level Checks**:

### 1. สร้าง Supabase Admin Client (`src/utils/supabase/admin.ts`)

สร้าง Client พิเศษที่ใช้ `SUPABASE_SERVICE_ROLE_KEY` ซึ่งมีสิทธิ์เข้าถึง Database เต็มรูปแบบ (Bypass RLS) สำหรับใช้งานเฉพาะฝั่ง Server เท่านั้น

### 2. เพิ่ม Application-Level Authorization Check (`admin-actions.ts`)

แทนที่จะพึ่งพา Database RLS เพียงอย่างเดียว เราย้ายการตรวจสอบสิทธิ์มาไว้ที่ Application Logic เพื่อความยืดหยุ่นและแน่นอนในระบบ Hybrid

**Before:**

```typescript
// เดิม: พึ่งพา RLS (ซึ่งล้มเหลวเพราะไม่มี Token)
const supabase = await createSupabaseServerClient();
// -> กลายเป็น Anon User -> โดน Block ❌
```

**After (New Standard):**

```typescript
// ใหม่: ตรวจสอบสิทธิ์ระดับแอพ แล้วใช้ Admin Client
const supabase = await getAuthenticatedAdminClient();

// Inside helper:
async function getAuthenticatedAdminClient() {
  const session = await auth(); // 1. เช็ค Login
  if (session.user.role !== "super_admin") {
    // 2. เช็ค Role อย่างเข้มงวด
    throw new Error("Permission Denied");
  }
  return createSupabaseAdminClient(); // 3. ให้ Client ที่มีสิทธิ์สูง ✅
}
```

### 3. ปรับปรุง UX หน้า Login (`src/app/login/page.tsx`)

- เพิ่ม Label ที่ชัดเจนให้กับปุ่ม Quick Access เพื่อป้องกันความสับสนระหว่างบัญชี "สมาชิก" (Public) และ "เหรัญญิก" (Admin)
- เพิ่ม Tooltip อธิบายสิทธิ์ของแต่ละบัญชี

## ✅ ผลลัพธ์ (Outcome)

1.  **แก้ปัญหา 42501:** เหรัญญิกสามารถสร้าง/แก้ไขข้อมูลได้ตามปกติ
2.  **ความปลอดภัยคงเดิม:** แม้จะ Bypass Database RLS แต่ถูกทดแทนด้วย Code-Level Permission Check ที่เข้มงวด (ต้องเป็น `super_admin` เท่านั้นถึงจะได้ Admin Client)
3.  **UX ที่ดีขึ้น:** ลดความสับสนในการใช้งานบัญชี และมี Error Message ที่เข้าใจง่าย

## 📝 รายการไฟล์ที่แก้ไข (Files Changed)

- `src/utils/supabase/admin.ts` (New)
- `src/app/(admin)/admin/_actions/admin-actions.ts` (Refactored Security Logic)
- `src/app/login/page.tsx` (UX Improvements)

---

**หมายเหตุ:** การแก้ไขนี้เป็นไปตามมาตรฐานความปลอดภัยสำหรับการพัฒนา Web Application แบบ Private Intranet ที่ต้องการความยืดหยุ่นสูง
