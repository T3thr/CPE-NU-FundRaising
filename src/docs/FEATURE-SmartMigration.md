# 🚀 Smart Migration Feature - เอกสารสรุปการพัฒนา

**วันที่:** 10 มกราคม 2569  
**เวอร์ชัน:** 2.0.0

---

## 📋 ภาพรวมโครงการ

### ความต้องการ (Requirements)

- ย้ายข้อมูลสถานะการเงินจาก Google Sheets เก่า (Dashboard + Records) มายังระบบ Supabase ใหม่
- รองรับทั้งการ Copy-Paste และการอัปโหลดไฟล์ .xlsx
- มี Smart Column Detection ที่ฉลาดระดับ AI
- UI/UX สวยงาม ใช้งานง่าย ตามมาตรฐาน STANDARD-TailwindCSS.md
- **[NEW v2.0]** แสดงจำนวนเงินที่ชำระแทน Checkbox
- **[NEW v2.0]** แก้ไขยอดเงินได้ทันทีจากตาราง (Inline Edit)
- **[NEW v2.0]** Year Sync ระหว่าง Migration Modal และ Payments Page

### เทคโนโลยีที่ใช้

- **Frontend:** Next.js 15, React 19, Framer Motion
- **Styling:** Tailwind CSS 4.0 (Inline Styles)
- **Backend:** Next.js Server Actions
- **Database:** Supabase (PostgreSQL)
- **File Parsing:** xlsx library (dynamic import)

---

## 🎯 ฟีเจอร์หลัก

### 1. Smart Column Detection (AI-like) 🧠

ระบบตรวจจับประเภทคอลัมน์อัตโนมัติจาก:

- **ชื่อหัวคอลัมน์** - เช่น "รหัสนิสิต", "จำนวนเงิน", "เดือน"
- **รูปแบบข้อมูล** - เช่น เลข 8 หลัก = รหัสนิสิต, URL = ลิงก์สลิป

| ประเภท       | Icon | ตัวอย่างการตรวจจับ         |
| ------------ | ---- | -------------------------- |
| รหัสนิสิต    | 🎓   | 66360001, รหัส, Student ID |
| ชื่อ-นามสกุล | 👤   | ชื่อ, Name, นายสมชาย       |
| คำนำหน้า     | 📛   | นาย, นางสาว, ด.ช.          |
| จำนวนเงิน    | 💰   | 70, 80, Amount             |
| เดือน        | 📅   | กรกฎาคม, ก.ค., July        |
| ปี           | 📆   | 68, 2568, Year             |
| ลิงก์สลิป    | 🔗   | drive.google.com           |
| วันที่/เวลา  | ⏰   | Timestamp, ประทับเวลา      |
| อีเมล        | 📧   | @nu.ac.th                  |

### 2. Multi-Input Support 📥

- **Copy & Paste:** วางข้อมูลจาก Google Sheets / Excel ได้โดยตรง
- **File Upload:** รองรับ .xlsx, .xls, .csv

### 3. 3-Step Wizard Process 📊

1. **Input:** วางข้อมูลหรืออัปโหลดไฟล์
2. **Mapping:** ตรวจสอบและแก้ไขการจับคู่คอลัมน์
3. **Preview:** ตรวจสอบข้อมูลก่อนนำเข้า

### 4. Smart Data Transformation 🔄

- แปลงชื่อเดือนภาษาไทย → ตัวเลข
- แปลงปี พ.ศ. → format ที่ระบบใช้
- สร้าง Members ใหม่อัตโนมัติถ้ายังไม่มีในระบบ
- ตรวจจับ Duplicate และข้ามโดยอัตโนมัติ

### 5. **[NEW v2.0]** Amount Display & Inline Edit 💰

- ตารางแสดง **จำนวนเงินที่ชำระจริง** ในแต่ละเดือน (฿70, ฿80 ฯลฯ) แทน ✓/✗
- **คลิกที่ช่องใดก็ได้** เพื่อแก้ไขยอดเงินทันที
- **Enter** เพื่อบันทึก, **Esc** เพื่อยกเลิก
- รองรับการเพิ่มยอดชำระใหม่ให้ช่องที่ยังว่าง

### 6. **[NEW v2.0]** Year Sync 📅

- ปีที่เลือกใน Payments Page จะ sync ไปยัง Migration Modal อัตโนมัติ
- ข้อมูลที่ไม่มีปีจะใช้ปีที่เลือกเป็นค่าเริ่มต้น

---

## 📁 ไฟล์ที่แก้ไข (v2.0)

### 1. admin-actions.ts

| Function                | Description                                  |
| ----------------------- | -------------------------------------------- |
| `getPaymentsGrid()`     | อัปเดตให้ return `amount` พร้อม `status`     |
| `updatePaymentAmount()` | แก้ไขจำนวนเงินของ payment ที่มีอยู่          |
| `upsertPayment()`       | เพิ่มหรืออัปเดต payment (ใช้กับ inline edit) |
| `migratePayments()`     | นำเข้าข้อมูลจำนวนมากจาก Google Sheets        |

### 2. PaymentsContent.tsx

**เปลี่ยนแปลงใน v2.0:**

- **Amount Display:** แสดงจำนวนเงินแทน checkmark
- **Inline Edit:** คลิกเพื่อแก้ไขยอดได้ทันที
- **Year Sync:** ส่ง `selectedYear` และ `monthlyFee` ไป Modal

**States ใหม่:**

```tsx
const [editingCell, setEditingCell] = useState<{
  memberId: string;
  month: number;
} | null>(null);
const [editAmount, setEditAmount] = useState("");
const [isSaving, setIsSaving] = useState(false);
```

### 3. SmartMigrationModal.tsx

**Props เพิ่มเติม:**

- `selectedYear` - ปีที่เลือกจาก PaymentsContent (sync)
- `monthlyFee` - ค่าธรรมเนียมต่อเดือน

---

## 🎨 UI/UX Design

### Amount Cell Display

| Status   | Display          | Background |
| -------- | ---------------- | ---------- |
| Verified | `฿70` หรือ `฿80` | Green 15%  |
| Pending  | ⏰ Icon          | Amber 15%  |
| Unpaid   | `0`              | Red 15%    |
| Future   | `-`              | Gray       |

### Inline Edit

- **Input:** Number field with blue border
- **Confirm:** Green ✓ button
- **Cancel:** Red ✗ button
- **Keyboard:** Enter=Save, Esc=Cancel

---

## 🔧 วิธีใช้งาน

### การแก้ไขยอดเงินแบบ Inline

1. ไปที่หน้า **Admin → การชำระเงิน**
2. **คลิกที่ช่อง** ที่ต้องการแก้ไข (แสดง `฿70`, `0`, ฯลฯ)
3. **พิมพ์ยอดเงินใหม่** ในช่อง input
4. กด **Enter** หรือ **ปุ่ม ✓** เพื่อบันทึก
5. กด **Esc** หรือ **ปุ่ม ✗** เพื่อยกเลิก

### การใช้ Smart Migration

1. **เปิด Google Sheets เก่า** ที่มี Records ของการชำระเงิน
2. **เลือกข้อมูล** ที่ต้องการย้าย (Ctrl+A หรือเลือกเฉพาะบางส่วน)
3. **Copy** ข้อมูล (Ctrl+C)
4. **ไปที่หน้า Payments** ในระบบใหม่ (`/admin/payments`)
5. **คลิกปุ่ม "Migration"** (ปุ่มสีม่วง-น้ำเงิน)
6. **เลือก "Copy & Paste"** และวางข้อมูล (Ctrl+V)
7. **ตรวจสอบ Column Mapping** - ระบบจะตรวจจับอัตโนมัติ
8. **คลิก "แปลงข้อมูล"** เพื่อดู Preview
9. **ตรวจสอบข้อมูล** - ดูว่าถูกต้องหรือไม่
10. **คลิก "นำเข้า"** เพื่อบันทึกลง Supabase

---

## ⚠️ หมายเหตุสำคัญ

1. **Duplicate Detection:** ระบบจะข้ามรายการที่ซ้ำ (studentId + month + year)
2. **Auto Member Creation:** ถ้าสมาชิกยังไม่มีในระบบ จะสร้างให้อัตโนมัติ
3. **Status = "verified":** ข้อมูลจากระบบเก่าถือว่าผ่านการตรวจสอบแล้ว
4. **Inline Edit:** การแก้ไขจะบันทึกทันที ไม่มี undo

---

## 📝 Changelog

### v2.0.0 (10 ม.ค. 2569)

#### Added

- 💰 **Amount Display** - แสดงจำนวนเงินที่ชำระในแต่ละเดือนแทน checkbox
- ✏️ **Inline Edit** - คลิกแก้ไขยอดเงินได้ทันที
- 🔗 **Year Sync** - ปีที่เลือกใน Payments จะ sync กับ Migration Modal
- 🆕 **upsertPayment()** - Server Action สำหรับ create/update payment
- 🆕 **updatePaymentAmount()** - Server Action สำหรับแก้ไขยอดเงิน

#### Changed

- **getPaymentsGrid()** - return ข้อมูลแบบ `{ id, status, amount }` แทน `string`
- **PaymentGridItem type** - ปรับโครงสร้างให้รองรับ amount

### v1.0.0 (10 ม.ค. 2569)

- 🚀 Initial release
- Smart Column Detection
- Multi-Input (Paste/Upload)
- 3-Step Wizard
- migratePayments Server Action

---

**สร้างโดย:** AI Assistant (Claude / Antigravity)  
**สำหรับ:** CPE30 Fund Management System
