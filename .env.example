# =============================================================================
# 🟢 APP CONFIGURATION
# =============================================================================
NODE_ENV=development
NEXT_PUBLIC_BASE_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:3000

# =============================================================================
# 🟢 SUPABASE (Backend & Auth)
# Docs: https://supabase.com/docs
# =============================================================================
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# SERVICE_ROLE_KEY is required for Admin tasks & Cron jobs (Do not expose to client)
SUPABASE_SERVICE_ROLE_KEY=

# =============================================================================
# 🟢 PAYMENT & BANKING (Public Display)
# =============================================================================
# Bank details shown on the payment page
NEXT_PUBLIC_BANK_NAME="KASIKORNTHAI"
NEXT_PUBLIC_BANK_ACCOUNT_NO="1931905372"
NEXT_PUBLIC_BANK_ACCOUNT_NAME="Theerapat Pooraya"

# =============================================================================
# 🟢 EXTERNAL SERVICES (Automation)
# =============================================================================

# EasySlip: For automatic slip verification (Free Tier: 50/week)
# Docs: https://developer.easyslip.com
EASYSLIP_API_KEY=

# -----------------------------------------------------------------------------
# LINE MESSAGING API (2026 Standard - Replaces deprecated Line Notify)
# Docs: https://developers.line.biz/en/docs/messaging-api/
# Setup:
#   1. Go to LINE Developers Console: https://developers.line.biz/console/
#   2. Create a Provider (if not exists)
#   3. Create a Messaging API Channel
#   4. Get Channel Access Token (long-lived) from Messaging API tab
#   5. Get Channel Secret from Basic settings
#   6. Add bot to group by scanning QR or LINE ID
# -----------------------------------------------------------------------------
LINE_CHANNEL_ACCESS_TOKEN=
LINE_CHANNEL_SECRET=
# Group ID to send notifications (get from webhook when bot joins group)
LINE_GROUP_ID=
# Alternative: User ID for 1-on-1 notifications
LINE_USER_ID=

# Upstash Redis: For rate limiting & caching (Optional)
# Docs: https://upstash.com
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Cron Job Secret: For securing cron job endpoints
# Generate a random string: openssl rand -hex 32
CRON_SECRET=

# =============================================================================
# 🟡 GOOGLE INTEGRATION (Migration & Backup)
# Used for syncing data with the old Google Sheets system
# =============================================================================
GOOGLE_CLIENT_EMAIL=
GOOGLE_CLIENT_ID=
# Private key should be single line with \n or handled carefully
GOOGLE_PRIVATE_KEY=
GOOGLE_SHEET_ID=
NEXT_PUBLIC_GOOGLE_SHEET_URL=

# =============================================================================
# 🔴 DEPRECATED - DO NOT USE
# =============================================================================
# LINE_NOTIFY_TOKEN= # Deprecated as of 2025-03. Use LINE Messaging API instead.
# STRIPE_SECRET_KEY= # Not used. System uses PromptPay/Slip.
